import { CapitaliseDirective } from './capitalise.directive';

describe('CapitaliseDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitaliseDirective();
    expect(directive).toBeTruthy();
  });
});
